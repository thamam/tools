# System Dependencies (install via package manager)
# FFmpeg is REQUIRED for audio processing with Whisper
# Ubuntu/Debian: sudo apt install ffmpeg
# CentOS/RHEL: sudo yum install ffmpeg
# macOS: brew install ffmpeg
# Windows: Download from https://ffmpeg.org/download.html

# Core dependencies
# keyboard>=0.13.5  # Replaced with pynput (see LINUX_HOTKEY_SOLUTION.md)
pynput>=1.8.0  # Cross-platform hotkeys without sudo
pyperclip>=1.9.0

# Audio recording
pyaudio>=0.2.14

# Speech recognition engines (at least one required)
openai-whisper>=20231117
SpeechRecognition>=3.10.0

# Additional dependencies
torch>=1.9.0
numpy>=1.21.0
requests>=2.25.0

# Audio feedback (currently used)
pygame>=2.0.0  # For audio playback and feedback sounds

# Resource monitoring (optional but recommended)
psutil>=5.9.0  # For resource monitoring

# Note: pyautogui removed due to Qt threading conflicts
# Use xdotool on Linux instead: sudo apt install xdotool

# Optional dependencies for wake word detection
# openwakeword>=0.6.0  # For wake word detection
# onnxruntime>=1.16.0  # Required for openWakeWord

# Optional TTS feedback (not currently used)
# pyttsx3>=2.90  # For text-to-speech feedback

# Development dependencies (for testing)
pytest>=7.0.0  # Test framework
pytest-cov>=4.0.0  # Coverage reporting
